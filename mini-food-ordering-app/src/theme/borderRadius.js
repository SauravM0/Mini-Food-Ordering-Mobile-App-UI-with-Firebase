// src/theme/borderRadius.js
const borderRadius = {
  // Standard radius for cards and primary buttons for a modern, soft appearance
  standard: 12,
  
  // Light radius for small badges, inputs, and minor UI elements
  light: 6,
};

export default borderRadius;