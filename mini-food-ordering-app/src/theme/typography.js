// src/theme/typography.js
const typography = {
  // Large size, bold weight - Used for screen titles like "Menu" and "Cart"
  heading: 24,
  
  // Medium size, semi-bold weight - For section headers and category names
  subheading: 18,
  
  // Medium size, regular weight - For descriptions and general text content
  body: 14,
  
  // Small size, regular weight - For labels, secondary info, and price units
  small: 12,
};

export default typography;